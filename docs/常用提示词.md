### 常用提示词

- 更新gitnexus索引，commit并push代码

- 阅读vitest相关技能，修复所有pnpm test报错及失败的测试用例，注意不要引入新的lint错误和类型错误。

- 使用vitest技能，改进测试用例，尽可能提高提升代码覆盖率

- 完整阅读并理解代码，进行研究和重构，注意符合AGENTS.md里要求的“重构原则”与“代码风格与架构原则”；

- 涉及数据流的改动追踪完整的端到端链路

- 自行在修改后使用单元测试以及E2E测试验证改动

- 修复 README.md 里所有待解决问题，并测试验证每一个修复是否生效

```bash
$env:Path = [System.Environment]::GetEnvironmentVariable("Path", "Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path", "User")
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:HTTP_PROXY="http://127.0.0.1:7890"
omp
```
